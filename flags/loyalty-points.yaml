apiVersion: unleashctl/v1
kind: Feature
metadata:
  name: loyalty-points
  service: payments
spec:
  type: release
  description: Loyalty points on checkout, gated to production by manual UI toggle
  enabled: true
  strategies:
    - name: flexibleRollout
      parameters: { rollout: "100", stickiness: userId, groupId: loyalty-points }
