apiVersion: unleashctl/v1
kind: Feature
metadata:
  name: email-verification
  service: other-repo
spec:
  type: release
  description: Require email verification before checkout (owned by a different repo/team)
  enabled: true
  strategies:
    - name: default
